# 🎰 Premium Casino Bot

Телеграм бот с мини-приложением для игры в слоты и рулетку на USDT.

## 🚀 Особенности

- **MongoDB** для хранения данных
- **Вывод только чеком** без адреса кошелька
- **Redis** для кэширования
- **Асинхронная архитектура** (aiogram 3.x + Quart)
- **Анимации и современный UI**
- **Админ-панель** со статистикой

## 📦 Структура проекта
.
├── bot.py                      # Основной файл бота
├── web_app/                    # Мини-приложение
│   ├── index.html
│   ├── css/
│   │   └── style.css
│   └── js/
│       └── app.js
├── requirements.txt            # Python зависимости
├── railway.json                # Конфигурация Railway
├── Dockerfile                  # Docker контейнер
└── .env.example               # Пример переменных окружения


## 🛠 Подготовка

1. **Создайте бота** в @BotFather
2. **Получите токен** в @CryptoBot
3. **Настройте MongoDB Atlas** (Free Tier)
4. **Добавьте переменные окружения** в Railway

## 📋 Переменные окружения

| Переменная | Описание | Где взять |
|------------|----------|-----------|
| `BOT_TOKEN` | Токен бота | @BotFather |
| `CRYPTO_PAY_TOKEN` | API токен | @CryptoBot |
| `MONGODB_URI` | URI MongoDB | MongoDB Atlas |
| `WEB_APP_URL` | URL мини-приложения | Railway (после деплоя) |
| `ADMIN_IDS` | ID админов | @userinfobot |

## 🚂 Деплой на Railway

1. Подключите GitHub репозиторий
2. Добавьте все переменные окружения
3. Нажмите "Deploy"
4. Обновите `WEB_APP_URL` в переменных
5. Перезапустите деплой

## 🎮 Использование

- `/start` - Запуск бота
- Играть - Открыть мини-приложение
- Профиль - Баланс, депозит, вывод
- Вывод - Создает чек в Crypto Pay

## 👨‍💻 Разработка

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск локально (требует .env)
python bot.py

📄 Лицензия
MIT License - используйте на свой страх и риск. Азартные игры могут быть незаконны в вашей юрисдикции.


---

## 📁 Создание структуры проекта

Выполните в терминале:

```bash
mkdir casino-bot
cd casino-bot
mkdir -p web_app/css web_app/js

# Создайте все файлы (скопируйте код выше в каждый файл):
# bot.py
# web_app/index.html
# web_app/css/style.css
# web_app/js/app.js
# requirements.txt
# railway.json
# Dockerfile
# .env.example
# README.md

# Инициализируйте Git
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/casino-bot.git
git push -u origin main

✅ Чек-лист перед деплоем
[ ] Все файлы созданы и заполнены
[ ] Создан публичный GitHub репозиторий
[ ] Получены все токены (Telegram, Crypto Pay)
[ ] Настроен MongoDB Atlas с доступом 0.0.0.0/0
[ ] В Railway добавлены все переменные окружения
[ ] WEB_APP_URL обновлен после первого деплоя
[ ] Бот добавлен в Telegram

🚀 Проект полностью готов к деплою!